{
  "name": "routeview-service",
  "version": "0.1.0",
  "description": "Route View API for access via native code.",
  "main": "server.js",
  "engines": {
    "node": ">=0.10.0",
    "npm": ">=1.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "James Wang <jw@slickage.com>",
  "license": "MIT",
  "dependencies": {
    "routes": "^0.2.0",
    "underscore": "^1.6.0",
    "request": "^2.34.0",
    "ejs": "^0.8.5",
    "error-page": "0.0.7",
    "templar": "^0.2.2"
  }
}
